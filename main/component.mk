#
# "main" pseudo-component makefile.
#
# (Uses default behaviour of compiling all source files in directory, adding 'include' to include path.)

COMPONENT_SRCDIRS += smartconfig_main
COMPONENT_ADD_INCLUDEDIRS += smartconfig_main/include

COMPONENT_SRCDIRS += station_example_main
COMPONENT_ADD_INCLUDEDIRS += station_example_main/include